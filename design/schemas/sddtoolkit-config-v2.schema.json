{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.sddtoolkit.dev/config/sddtoolkit-config-v2.schema.json",
  "title": "SDDToolKit configuration v2",
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "logs", "models", "paths"],
  "properties": {
    "version": { "const": "2.0" },
    "logs": { "$ref": "#/$defs/logs" },
    "models": { "$ref": "#/$defs/models" },
    "paths": { "$ref": "#/$defs/paths" }
  },
  "$defs": {
    "logs": {
      "type": "object",
      "additionalProperties": false,
      "required": ["level", "console", "promptCapture"],
      "properties": {
        "level": { "type": "string" },
        "console": { "type": "boolean" },
        "promptCapture": {
          "type": "array",
          "maxItems": 4,
          "uniqueItems": true,
          "items": {
            "enum": ["request", "response", "reference_body", "code_body"]
          }
        }
      }
    },
    "models": {
      "type": "object",
      "additionalProperties": false,
      "required": ["slots"],
      "properties": {
        "slots": {
          "type": "object",
          "additionalProperties": { "$ref": "#/$defs/modelSlot" }
        }
      }
    },
    "modelSlot": {
      "type": "object",
      "additionalProperties": false,
      "required": ["provider", "model"],
      "properties": {
        "provider": { "type": "string" },
        "model": { "type": "string" },
        "reasoningEffort": { "type": "string" }
      }
    },
    "paths": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "specs",
        "references",
        "specsArchive",
        "workflows",
        "toolchainPreset",
        "principles",
        "templates"
      ],
      "properties": {
        "specs": { "type": "string" },
        "references": { "type": "string" },
        "specsArchive": { "type": "string" },
        "workflows": { "type": "string" },
        "toolchainPreset": { "type": "string" },
        "principles": { "type": "string" },
        "templates": { "type": "string" }
      }
    }
  }
}
