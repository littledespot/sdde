schema: workflow/v1
id: spec-generation
version: 1
shortcode: SGEN
invoke: specify-invocation
policy: core.specification-generation@1
start: capture-project
resources:
  extraction-prompt: spec/extraction.prompt.md
  extraction-schema: spec/extraction.schema.json
  reconciliation-prompt: spec/reconciliation.prompt.md
  reconciliation-schema: spec/reconciliation.schema.json
  generation-prompt: spec/generation.prompt.md
  generation-schema: spec/generation.schema.json
  support-prompt: spec/support.prompt.md
  support-schema: spec/support.schema.json
steps:
  capture-project: { use: capture-project-toolchain, on: { ok: inventory-presets, failed: end.failed } }
  inventory-presets: { use: inventory-toolchain-presets, on: { ok: capture-presets, failed: end.failed } }
  capture-presets: { use: capture-toolchain-presets, on: { ok: parse-toolchain, failed: end.failed } }
  parse-toolchain: { use: parse-toolchain-documents, on: { ok: validate-project, failed: end.failed } }
  validate-project: { use: validate-project-toolchain-schema, on: { ok: validate-registry, failed: end.failed } }
  validate-registry: { use: validate-toolchain-preset-registry, on: { ok: inherit, failed: end.failed } }
  inherit: { use: resolve-toolchain-inheritance, on: { ok: compose, failed: end.failed } }
  compose: { use: compose-toolchain, on: { ok: validate-safety, failed: end.failed } }
  validate-safety: { use: validate-toolchain-safety, on: { ok: normalize-feature, failed: end.failed } }
  normalize-feature: { use: normalize-feature-directory, on: { ok: validate-feature, failed: end.failed } }
  validate-feature: { use: validate-feature-directory, on: { ok: inspect-feature, failed: end.failed } }
  inspect-feature: { use: inspect-feature-directory, on: { ok: normalize-reference, failed: end.failed } }
  normalize-reference: { use: normalize-reference-selector, on: { ok: validate-reference, failed: end.failed } }
  validate-reference: { use: validate-reference-selector, on: { ok: inspect-reference, failed: end.failed } }
  inspect-reference: { use: inspect-reference-directory, on: { ok: resolve-paths, failed: end.failed } }
  resolve-paths: { use: resolve-feature-artifact-paths, on: { ok: capture-clarifications, failed: end.failed } }
  capture-clarifications: { use: capture-clarification-inputs, on: { ok: parse-state, failed: end.failed } }
  parse-state: { use: parse-clarification-state, on: { ok: validate-state, failed: end.failed } }
  validate-state: { use: validate-clarification-state, on: { ok: validate-forms, failed: end.failed } }
  validate-forms: { use: validate-clarification-forms, on: { ok: inventory-references, failed: end.failed } }
  inventory-references: { use: inventory-reference-sources, on: { ok: validate-inventory, failed: end.failed } }
  validate-inventory: { use: validate-reference-inventory, on: { ok: capture-references, failed: end.failed } }
  capture-references: { use: capture-reference-sources, on: { ok: decode-references, failed: end.failed } }
  decode-references: { use: decode-reference-markdown, on: { ok: validate-accounting, failed: end.failed } }
  validate-accounting: { use: validate-reference-accounting, on: { ok: identify-references, failed: end.failed } }
  identify-references: { use: assign-reference-identities, on: { ok: build-chunks, failed: end.failed } }
  build-chunks: { use: build-reference-chunks, on: { ok: validate-chunks, failed: end.failed } }
  validate-chunks: { use: validate-reference-chunks, on: { ok: compile-naming, failed: end.failed } }
  compile-naming: { use: compile-naming-policy, on: { ok: build-grammar, failed: end.failed } }
  build-grammar: { use: build-superset-path-token-grammar, on: { ok: scan-literals, failed: end.failed } }
  scan-literals: { use: scan-reference-passive-literals, on: { ok: assign-literals, failed: end.failed } }
  assign-literals: { use: assign-passive-literal-identities, on: { ok: validate-literals, failed: end.failed } }
  validate-literals: { use: validate-reference-passive-literals, on: { ok: extract-facts, failed: end.failed } }
  extract-facts: { use: extract-structured-reference-facts, on: { ok: identify-tokens, failed: end.failed } }
  identify-tokens: { use: assign-structured-token-candidate-identities, on: { ok: initialize-requests, failed: end.failed } }
  initialize-requests: { use: build-initial-model-request-identity-ledger, on: { ok: initialize-extraction, failed: end.failed } }
  initialize-extraction: { use: initialize-reference-extraction, on: { ok: check-extraction, failed: end.failed } }
  check-extraction: { use: check-reference-extraction-progress, on: { ok: finish-extraction, more: extraction-packet, failed: end.failed } }
  extraction-packet: { use: build-reference-extraction-model-input, on: { ok: ex-assign, failed: end.failed } }
  ex-assign: { use: assign-model-request-id, with: { slot: spec_generation, response-mode: prompt-only, prompt: extraction-prompt, result-schema: extraction-schema }, on: { ok: ex-validate, failed: end.failed } }
  ex-validate: { use: validate-model-request-binding, on: { ok: ex-build, failed: end.failed } }
  ex-build: { use: build-model-request, on: { ok: ex-account, failed: end.failed } }
  ex-account: { use: advance-model-attempt-accounting, with: { retry-limit: 128 }, on: { ok: ex-operation, failed: end.failed } }
  ex-operation: { use: assign-provider-operation, with: { kind: inference }, on: { ok: ex-authorize, failed: end.failed } }
  ex-authorize: { use: prepare-provider-operation-authorization, with: { timeout-ms: 60000 }, on: { ok: ex-advance-request, failed: ex-terminate-operation, cancelled: ex-terminate-operation } }
  ex-advance-request: { use: advance-model-request-lifecycle, with: { transition: invoked }, on: { ok: ex-advance-operation, failed: end.failed } }
  ex-advance-operation: { use: advance-provider-operation-lifecycle, with: { transition: invoked }, on: { ok: ex-call, failed: end.failed } }
  ex-call: { use: invoke-model, on: { ok: ex-observation, failed: ex-observation, cancelled: ex-observation } }
  ex-observation: { use: validate-provider-invocation-observation, on: { ok: ex-complete-operation, failed: ex-complete-operation, cancelled: ex-complete-operation } }
  ex-complete-operation: { use: complete-provider-operation, on: { ok: ex-decode, failed: ex-decode, cancelled: ex-decode } }
  ex-decode: { use: decode-model-envelope, on: { ok: ex-payload, invalid: ex-payload, failed: ex-payload, cancelled: ex-payload } }
  ex-payload: { use: validate-model-payload-schema, on: { ok: ex-close, invalid: ex-close, failed: ex-close, cancelled: ex-close } }
  ex-close: { use: complete-model-request, on: { ok: collect-extraction, invalid: end.invalid, failed: end.failed, cancelled: end.cancelled } }
  ex-terminate-operation: { use: terminate-provider-operation, on: { failed: ex-terminate-request, cancelled: ex-terminate-request } }
  ex-terminate-request: { use: terminate-model-request, on: { failed: end.failed, cancelled: end.cancelled } }
  collect-extraction: { use: collect-reference-extraction-result, on: { ok: ex-retire-input, failed: end.failed } }
  ex-retire-input: { use: retire-model-input, on: { ok: ex-retire, failed: end.failed } }
  ex-retire: { use: retire-model-request, on: { ok: check-extraction, failed: end.failed } }
  finish-extraction: { use: build-reference-extraction-results, on: { ok: parse-extraction, failed: end.failed } }
  parse-extraction: { use: parse-reference-extraction-results, on: { ok: validate-extraction-text, failed: end.failed } }
  validate-extraction-text: { use: validate-reference-extraction-text, on: { ok: classify-tokens, failed: end.failed } }
  classify-tokens: { use: validate-preserved-token-classifications, on: { ok: assign-tokens, failed: end.failed } }
  assign-tokens: { use: assign-preserved-token-identities, on: { ok: build-token-claims, failed: end.failed } }
  build-token-claims: { use: build-preserved-token-claims, on: { ok: validate-claims, failed: end.failed } }
  validate-claims: { use: validate-reference-claims, on: { ok: assign-claims, failed: end.failed } }
  assign-claims: { use: assign-reference-claim-identities, on: { ok: build-ledger, failed: end.failed } }
  build-ledger: { use: build-reference-extraction-ledger, on: { ok: account-extraction, failed: end.failed } }
  account-extraction: { use: validate-reference-extraction-accounting, on: { ok: build-reconciliation, blocked: end.blocked, failed: end.failed } }
  build-reconciliation: { use: build-reference-reconciliation-items, on: { ok: partition-reconciliation, failed: end.failed } }
  partition-reconciliation: { use: partition-reference-reconciliation-items, with: { group-size: 8 }, on: { ok: assign-partitions, failed: end.failed } }
  assign-partitions: { use: assign-reference-reconciliation-partitions, on: { ok: validate-partitions, failed: end.failed } }
  validate-partitions: { use: validate-reference-reconciliation-partitions, on: { ok: reconciliation-input, failed: end.failed } }
  reconciliation-input: { use: build-reference-reconciliation-input, on: { ok: reconciliation-packet, failed: end.failed } }
  reconciliation-packet: { use: build-reference-reconciliation-model-input, on: { ok: rc-assign, failed: end.failed } }
  rc-assign: { use: assign-model-request-id, with: { slot: spec_generation, response-mode: prompt-only, prompt: reconciliation-prompt, result-schema: reconciliation-schema }, on: { ok: rc-validate, failed: end.failed } }
  rc-validate: { use: validate-model-request-binding, on: { ok: rc-build, failed: end.failed } }
  rc-build: { use: build-model-request, on: { ok: rc-account, failed: end.failed } }
  rc-account: { use: advance-model-attempt-accounting, with: { retry-limit: 128 }, on: { ok: rc-operation, failed: end.failed } }
  rc-operation: { use: assign-provider-operation, with: { kind: inference }, on: { ok: rc-authorize, failed: end.failed } }
  rc-authorize: { use: prepare-provider-operation-authorization, with: { timeout-ms: 60000 }, on: { ok: rc-advance-request, failed: rc-terminate-operation, cancelled: rc-terminate-operation } }
  rc-advance-request: { use: advance-model-request-lifecycle, with: { transition: invoked }, on: { ok: rc-advance-operation, failed: end.failed } }
  rc-advance-operation: { use: advance-provider-operation-lifecycle, with: { transition: invoked }, on: { ok: rc-call, failed: end.failed } }
  rc-call: { use: invoke-model, on: { ok: rc-observation, failed: rc-observation, cancelled: rc-observation } }
  rc-observation: { use: validate-provider-invocation-observation, on: { ok: rc-complete-operation, failed: rc-complete-operation, cancelled: rc-complete-operation } }
  rc-complete-operation: { use: complete-provider-operation, on: { ok: rc-decode, failed: rc-decode, cancelled: rc-decode } }
  rc-decode: { use: decode-model-envelope, on: { ok: rc-payload, invalid: rc-payload, failed: rc-payload, cancelled: rc-payload } }
  rc-payload: { use: validate-model-payload-schema, on: { ok: rc-close, invalid: rc-close, failed: rc-close, cancelled: rc-close } }
  rc-close: { use: complete-model-request, on: { ok: collect-reconciliation, invalid: end.invalid, failed: end.failed, cancelled: end.cancelled } }
  rc-terminate-operation: { use: terminate-provider-operation, on: { failed: rc-terminate-request, cancelled: rc-terminate-request } }
  rc-terminate-request: { use: terminate-model-request, on: { failed: end.failed, cancelled: end.cancelled } }
  collect-reconciliation: { use: collect-reference-reconciliation-result, on: { ok: rc-retire-input, failed: end.failed } }
  rc-retire-input: { use: retire-model-input, on: { ok: rc-retire, failed: end.failed } }
  rc-retire: { use: retire-model-request, on: { ok: parse-reconciliation, failed: end.failed } }
  parse-reconciliation: { use: parse-reference-reconciliation-result, on: { ok: check-reconciliation, failed: end.failed } }
  check-reconciliation: { use: check-reference-reconciliation-purpose, on: { ok: validate-dispositions, more: validate-summary, failed: end.failed } }
  validate-summary: { use: validate-reference-reconciliation-summary, on: { ok: assign-summary, failed: end.failed } }
  assign-summary: { use: assign-reference-summary-identities, on: { ok: build-summary, failed: end.failed } }
  build-summary: { use: build-reference-reconciliation-summary, on: { ok: reconciliation-input, failed: end.failed } }
  validate-dispositions: { use: validate-reference-claim-dispositions, on: { ok: validate-signals, failed: end.failed } }
  validate-signals: { use: validate-reference-signal-proposals, on: { ok: validate-conflicts, failed: end.failed } }
  validate-conflicts: { use: validate-reference-conflict-proposals, on: { ok: assign-records, failed: end.failed } }
  assign-records: { use: assign-reference-reconciliation-identities, on: { ok: build-records, failed: end.failed } }
  build-records: { use: build-reference-reconciliation-records, on: { ok: account-reconciliation, failed: end.failed } }
  account-reconciliation: { use: validate-reference-reconciliation-completeness, on: { ok: pre-project, blocked: end.blocked, failed: end.failed } }
  pre-project: { use: build-specification-authority-requirements, on: { ok: pre-input, blocked: end.blocked } }
  pre-input: { use: build-specification-support-input, on: { ok: pre-assign, failed: end.failed } }
  pre-assign: { use: assign-model-request-id, with: { slot: spec_generation, response-mode: prompt-only, prompt: support-prompt, result-schema: support-schema }, on: { ok: pre-validate, failed: end.failed } }
  pre-validate: { use: validate-model-request-binding, on: { ok: pre-build, failed: end.failed } }
  pre-build: { use: build-model-request, on: { ok: pre-account, failed: end.failed } }
  pre-account: { use: advance-model-attempt-accounting, with: { retry-limit: 0 }, on: { ok: pre-operation, failed: end.failed } }
  pre-operation: { use: assign-provider-operation, with: { kind: inference }, on: { ok: pre-authorize, failed: end.failed } }
  pre-authorize: { use: prepare-provider-operation-authorization, with: { timeout-ms: 60000 }, on: { ok: pre-advance-request, failed: pre-terminate-operation, cancelled: pre-terminate-operation } }
  pre-advance-request: { use: advance-model-request-lifecycle, with: { transition: invoked }, on: { ok: pre-advance-operation, failed: end.failed } }
  pre-advance-operation: { use: advance-provider-operation-lifecycle, with: { transition: invoked }, on: { ok: pre-call, failed: end.failed } }
  pre-call: { use: invoke-model, on: { ok: pre-observation, failed: pre-observation, cancelled: pre-observation } }
  pre-observation: { use: validate-provider-invocation-observation, on: { ok: pre-complete-operation, failed: pre-complete-operation, cancelled: pre-complete-operation } }
  pre-complete-operation: { use: complete-provider-operation, on: { ok: pre-decode, failed: pre-decode, cancelled: pre-decode } }
  pre-decode: { use: decode-model-envelope, on: { ok: pre-payload, invalid: pre-payload, failed: pre-payload, cancelled: pre-payload } }
  pre-payload: { use: validate-model-payload-schema, on: { ok: pre-close, invalid: pre-close, failed: pre-close, cancelled: pre-close } }
  pre-close: { use: complete-model-request, on: { ok: pre-collect, invalid: end.invalid, failed: end.failed, cancelled: end.cancelled } }
  pre-terminate-operation: { use: terminate-provider-operation, on: { failed: pre-terminate-request, cancelled: pre-terminate-request } }
  pre-terminate-request: { use: terminate-model-request, on: { failed: end.failed, cancelled: end.cancelled } }
  pre-collect: { use: collect-specification-support, on: { ok: pre-retire-input, failed: end.failed } }
  pre-retire-input: { use: retire-model-input, on: { ok: pre-retire, failed: end.failed } }
  pre-retire: { use: retire-model-request, on: { ok: pre-ledger, failed: end.failed } }
  pre-ledger: { use: build-required-authority-ledger, on: { ok: pre-observations, blocked: end.blocked } }
  pre-observations: { use: build-required-authority-observations, on: { ok: pre-reconcile, failed: end.failed } }
  pre-reconcile: { use: reconcile-required-authorities, on: { ok: pre-gate, needs_user: pre-gate, blocked: pre-gate } }
  pre-gate: { use: validate-required-authority-reconciliation, on: { ok: initialize-specification, needs_user: end.needs_user, blocked: end.blocked } }
  initialize-specification: { use: initialize-specification-generation, on: { ok: check-specification, failed: end.failed } }
  check-specification: { use: check-specification-generation-progress, on: { ok: assemble-specification, more: generation-input, failed: end.failed } }
  generation-input: { use: build-specification-model-input, on: { ok: gen-assign, failed: end.failed } }
  gen-assign: { use: assign-model-request-id, with: { slot: spec_generation, response-mode: prompt-only, prompt: generation-prompt, result-schema: generation-schema }, on: { ok: gen-validate, failed: end.failed } }
  gen-validate: { use: validate-model-request-binding, on: { ok: gen-build, failed: end.failed } }
  gen-build: { use: build-model-request, on: { ok: gen-account, failed: end.failed } }
  gen-account: { use: advance-model-attempt-accounting, with: { retry-limit: 64 }, on: { ok: gen-operation, failed: end.failed } }
  gen-operation: { use: assign-provider-operation, with: { kind: inference }, on: { ok: gen-authorize, failed: end.failed } }
  gen-authorize: { use: prepare-provider-operation-authorization, with: { timeout-ms: 60000 }, on: { ok: gen-advance-request, failed: gen-terminate-operation, cancelled: gen-terminate-operation } }
  gen-advance-request: { use: advance-model-request-lifecycle, with: { transition: invoked }, on: { ok: gen-advance-operation, failed: end.failed } }
  gen-advance-operation: { use: advance-provider-operation-lifecycle, with: { transition: invoked }, on: { ok: gen-call, failed: end.failed } }
  gen-call: { use: invoke-model, on: { ok: gen-observation, failed: gen-observation, cancelled: gen-observation } }
  gen-observation: { use: validate-provider-invocation-observation, on: { ok: gen-complete-operation, failed: gen-complete-operation, cancelled: gen-complete-operation } }
  gen-complete-operation: { use: complete-provider-operation, on: { ok: gen-decode, failed: gen-decode, cancelled: gen-decode } }
  gen-decode: { use: decode-model-envelope, on: { ok: gen-payload, invalid: gen-payload, failed: gen-payload, cancelled: gen-payload } }
  gen-payload: { use: validate-model-payload-schema, on: { ok: gen-close, invalid: gen-close, failed: gen-close, cancelled: gen-close } }
  gen-close: { use: complete-model-request, on: { ok: collect-specification, invalid: end.invalid, failed: end.failed, cancelled: end.cancelled } }
  gen-terminate-operation: { use: terminate-provider-operation, on: { failed: gen-terminate-request, cancelled: gen-terminate-request } }
  gen-terminate-request: { use: terminate-model-request, on: { failed: end.failed, cancelled: end.cancelled } }
  collect-specification: { use: collect-specification-model-result, on: { ok: parse-specification, failed: end.failed } }
  parse-specification: { use: parse-specification-unit, on: { ok: validate-specification, invalid: end.invalid, failed: end.failed } }
  validate-specification: { use: validate-specification-unit, on: { ok: advance-specification, invalid: end.invalid, needs_user: end.needs_user, failed: end.failed } }
  advance-specification: { use: advance-specification-generation, on: { ok: gen-retire-input, failed: end.failed } }
  gen-retire-input: { use: retire-model-input, on: { ok: gen-retire, failed: end.failed } }
  gen-retire: { use: retire-model-request, on: { ok: check-specification, failed: end.failed } }
  assemble-specification: { use: assemble-specification-content, on: { ok: validate-coverage, failed: end.failed } }
  validate-coverage: { use: validate-specification-coverage, on: { ok: retire-pre-authority, invalid: end.invalid, failed: end.failed } }
  retire-pre-authority: { use: retire-required-authority-projection, on: { ok: post-project, failed: end.failed } }
  post-project: { use: build-specification-authority-requirements, on: { ok: post-input, blocked: end.blocked } }
  post-input: { use: build-specification-support-input, on: { ok: post-assign, failed: end.failed } }
  post-assign: { use: assign-model-request-id, with: { slot: spec_generation, response-mode: prompt-only, prompt: support-prompt, result-schema: support-schema }, on: { ok: post-validate, failed: end.failed } }
  post-validate: { use: validate-model-request-binding, on: { ok: post-build, failed: end.failed } }
  post-build: { use: build-model-request, on: { ok: post-account, failed: end.failed } }
  post-account: { use: advance-model-attempt-accounting, with: { retry-limit: 0 }, on: { ok: post-operation, failed: end.failed } }
  post-operation: { use: assign-provider-operation, with: { kind: inference }, on: { ok: post-authorize, failed: end.failed } }
  post-authorize: { use: prepare-provider-operation-authorization, with: { timeout-ms: 60000 }, on: { ok: post-advance-request, failed: post-terminate-operation, cancelled: post-terminate-operation } }
  post-advance-request: { use: advance-model-request-lifecycle, with: { transition: invoked }, on: { ok: post-advance-operation, failed: end.failed } }
  post-advance-operation: { use: advance-provider-operation-lifecycle, with: { transition: invoked }, on: { ok: post-call, failed: end.failed } }
  post-call: { use: invoke-model, on: { ok: post-observation, failed: post-observation, cancelled: post-observation } }
  post-observation: { use: validate-provider-invocation-observation, on: { ok: post-complete-operation, failed: post-complete-operation, cancelled: post-complete-operation } }
  post-complete-operation: { use: complete-provider-operation, on: { ok: post-decode, failed: post-decode, cancelled: post-decode } }
  post-decode: { use: decode-model-envelope, on: { ok: post-payload, invalid: post-payload, failed: post-payload, cancelled: post-payload } }
  post-payload: { use: validate-model-payload-schema, on: { ok: post-close, invalid: post-close, failed: post-close, cancelled: post-close } }
  post-close: { use: complete-model-request, on: { ok: post-collect, invalid: end.invalid, failed: end.failed, cancelled: end.cancelled } }
  post-terminate-operation: { use: terminate-provider-operation, on: { failed: post-terminate-request, cancelled: post-terminate-request } }
  post-terminate-request: { use: terminate-model-request, on: { failed: end.failed, cancelled: end.cancelled } }
  post-collect: { use: collect-specification-support, on: { ok: post-retire-input, failed: end.failed } }
  post-retire-input: { use: retire-model-input, on: { ok: post-retire, failed: end.failed } }
  post-retire: { use: retire-model-request, on: { ok: post-ledger, failed: end.failed } }
  post-ledger: { use: build-required-authority-ledger, on: { ok: post-observations, blocked: end.blocked } }
  post-observations: { use: build-required-authority-observations, on: { ok: post-reconcile, failed: end.failed } }
  post-reconcile: { use: reconcile-required-authorities, on: { ok: post-gate, needs_user: post-gate, blocked: post-gate } }
  post-gate: { use: validate-required-authority-reconciliation, on: { ok: end.ok, needs_user: end.needs_user, blocked: end.blocked } }
