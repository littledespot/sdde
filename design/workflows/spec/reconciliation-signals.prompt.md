Return supported signals for the supplied claims using prerequisite dispositions.
Cover every retained claim. Keep business content and preserved tokens separate;
each nonconflicting token needs its own exact-token signal.
