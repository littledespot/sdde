# FIX_002 — Source preservation, semantic review and actionable clarifications

**Date:** 21 September 2026. **Design review updated:** 22 September 2026.

**Status:** Phases 0–4 implementation is verified offline. The approved Phase 6 run handled entity applicability correctly but asked seven broader questions before generation; no completed specification or grade. Phase 4 live acceptance and Phase 6 closure remain open; see §§12–13.

**Scope:** Remediate the observed false-conflict → unsupported-review → unnecessary-clarification sequence through existing owners.

This plan follows an end-to-end inspection of the retained run, requests,
responses, production events, published artifacts, current implementation and
governing contracts. The user's 22 September documentation request strengthens the
shared [§12.8.1 contract](../design/contracts/12-model-boundary.md#1281-clarification-admission-and-candidate-failure-precedence).
This plan tracks that change and the subsequently authorized implementation; it
grants no model-call approval. Optional verdict reassessment remains unapproved.
[FIX_001](FIX_001.md) and [IMP_001](IMP_001.md) retain historical delivery evidence;
this document records the follow-up sequence, not another source of runtime policy.

## 1. Required outcome

Generate a source-grounded business specification when the references establish
sufficient intent. Ask the user only for an identifiable missing decision. Treat
meaning lost through extraction or reconciliation as a candidate defect, with
repair only through established authorization and complete dependent rebuilding.

Preserve these boundaries throughout the rollout:

- Unrepairable JSON/schema failure terminates with an error and no new `spec.md`,
  including when clarifications are already open.
- A genuine clarification pause may publish a validated incomplete specification;
  it cannot record completed specification authority or run completed-spec grading.
- Unresolved candidate/interpretation failure cannot become a clarification merely
  because question text is well formed or another genuine gap exists. Apply §12.8.1
  before creating needs or publishing; preserve prior artifacts on terminal failure.
- Open Spec clarifications prevent Plan; open Spec or Plan clarifications prevent Tasks.
- References and accepted answers establish business intent. Principles supply policy
  evidence; they cannot invent, erase or replace that intent.
- No greeting, timestamp, filename, model name or fixture-specific continuation rule.
  The illustrative specification discussed with the user is not a golden document.
- Preserve configured JSON composition, full validation, native provenance, local
  retry identities and actual-token accounting against the execution budget.
- No new fixer agent, semantic keyword detector, schema registry, evidence store,
  retry engine, Markdown repair path or automatic provider/model fallback.

Authority: [design §§1–4](../design/design.md#1-executive-summary),
[§16.4](../design/contracts/16-reference-ingestion.md#164-semantic-extraction-flow),
[§17](../design/contracts/17-specify.md), [§22](../design/contracts/22-repair.md),
[§23.2](../design/contracts/23-rendering.md#232-workflow-reruns-and-protected-clarification-files),
[ADR 0015](../design/decisions/0015-specification-principle-review.md),
[ADR 0017](../design/decisions/0017-incomplete-specification-publication.md),
and design §31 acceptance criteria 12–22.

## 2. Evidence baseline

### 2.1 Execution and measurements

Retained execution:
[`2026-09-21T09-53-34Z-40984295e35d0b12272feeb8997f74b5`](../zig-out/e2e-spec/2026-09-21T09-53-34Z-40984295e35d0b12272feeb8997f74b5/report.json).

| Observation | Recorded value |
| --- | --- |
| Result | `awaiting_clarification` / `needs_user` |
| Terminal step | `g22-g11-source-gaps-output-publish` |
| Publication / grading | Publication check passed; semantic quality `not_run` |
| Provider/model | AWS Bedrock `openai.gpt-oss-20b-1:0`, `ap-southeast-2` |
| Request configuration | Native `json_schema`; temperature `0`; reasoning effort `low` |
| Calls / usage | 17 calls; 30,839 / 100,000 tokens; usage complete |
| Unresolved protocol rejection | None; JSON/schema error and retry-error report fields are null |
| Logged normalization | 15 `REMOVED_LEADING_BRACE_QUOTE` events |
| Published state | `spec_clarification_pending`, with S01–S09 |
| Executed build | Zig 0.16.0, Debug, aarch64-macos-none |
| Executed source identity | Revision `bae126f177b61fb66454980add801c189ab28f99`, modified tree, source SHA-256 `c6c399b9e4ef7810f162c6c5fef5055d53cd4a2879cdb6bf10224b540776398b` |

The inspection checkout was clean at revision
`90d48e1ad394f4fa6ee046e2e0cd8e7dbf3de1a6` before this document was created.
All 29 files listed in the current harness case matched their retained target-project
copies byte-for-byte. The executed build's modified-tree identity is recorded above;
do not equate its revision alone with the later inspection checkout.

Provider-reported usage, summed from the retained `response.json` files:

| Work | Calls | Input tokens | Output tokens | Total |
| --- | ---: | ---: | ---: | ---: |
| Extraction | 2 | 3,100 | 301 | 3,401 |
| Hierarchical summaries | 2 | 4,938 | 487 | 5,425 |
| Global reconciliation slices | 3 | 6,292 | 486 | 6,778 |
| Initial source review | 1 | 2,424 | 879 | 3,303 |
| Selected question repairs | 9 | 10,916 | 1,016 | 11,932 |
| **Total** | **17** | **27,670** | **3,169** | **30,839** |

The nine question repairs consumed 11,932 tokens without correcting the semantic
diagnosis. This is observed cost, not a predicted saving from a proposed change.
The captured initial source-review `request.json` is 14,212 bytes; the nine repair
request files total 50,789 bytes. These are captured JSON-file sizes, not token
estimates or claims about HTTP framing size.

### 2.2 Failure sequence

1. **Source capture:** the original title and all three requirements were present:
   successful startup, exact greeting, and UTC date/time output. The source review
   received the complete captured source; missing input is not established here.
2. **Extraction, call 1:** one business claim preserved the three requirements.
   Another claim repeated the greeting/date-time behaviour under `technical`.
   Classification duplicated meaning; it did not remove the source requirements.
3. **Token classification, call 2:** the greeting became an exact business token.
   Calls 3–4 retained the claims through hierarchical summaries.
4. **Dispositions, call 5:** the model marked business claim 1 and technical claim 2
   reciprocally `conflicting`. Their supplied meanings are compatible.
5. **Signals/conflicts, calls 6–7:** signals retained only the exact token. An
   identical duplicate signal was removed through native repair. The conflict
   record classified claims 1–2 as `mutually_exclusive`; its summary concatenated
   their meanings rather than explaining an incompatibility.
6. **Source review, call 8:** nine of eleven findings were `unsupported`, including
   feature name, user-visible behaviour, pass/fail outcomes and required behaviour.
   The nine findings lacked `question`. The other two findings supported token
   preservation. All loss locations were `unlocalized`.
7. **Native review repair, calls 9–17:** each call replaced one finding's
   `detail`/`question` pair. These were distinct selected targets, not nine protocol
   retries of the same response. The retained negative decisions were unchanged.
8. **Authority gate:** the admitted negative findings produced `needs_user`.
   The source-omission repair route was not selected.
9. **Publication:** the incomplete renderer excluded conflicting business claims.
   With only a preserved token retained, it emitted no supported business content
   and linked nine questions. Specification generation, candidate principle
   assessment and completed-spec grading were not reached.

Primary evidence:
[extraction](../zig-out/e2e-spec/2026-09-21T09-53-34Z-40984295e35d0b12272feeb8997f74b5/evidence/generation/call-000001/model_output.txt),
[conflict](../zig-out/e2e-spec/2026-09-21T09-53-34Z-40984295e35d0b12272feeb8997f74b5/evidence/generation/call-000007/model_output.txt),
[review request](../zig-out/e2e-spec/2026-09-21T09-53-34Z-40984295e35d0b12272feeb8997f74b5/evidence/generation/call-000008/context.json),
[review response](../zig-out/e2e-spec/2026-09-21T09-53-34Z-40984295e35d0b12272feeb8997f74b5/evidence/generation/call-000008/model_output.txt),
[selected repair](../zig-out/e2e-spec/2026-09-21T09-53-34Z-40984295e35d0b12272feeb8997f74b5/evidence/generation/call-000010/context.json),
[S03](../zig-out/e2e-spec/2026-09-21T09-53-34Z-40984295e35d0b12272feeb8997f74b5/project/specs/hello-world/clarify/S03.md),
[published specification](../zig-out/e2e-spec/2026-09-21T09-53-34Z-40984295e35d0b12272feeb8997f74b5/project/specs/hello-world/spec.md).

S03 exposes the semantic failure directly: its reason states the greeting and UTC
behaviour, while its question asks the user to supply the missing user-visible
behaviour and specify an answer format.

## 3. Causal findings and limits

### Confirmed architectural contributors

- **One business claim carries all three behaviours.** The false conflict therefore
  strands startup, greeting and date/time together. This establishes the impact of
  the chosen granularity, not that grouping caused the model's conflict judgment.
- **Candidate labels also restrict diagnostic evidence.**
  `specification_support_evidence.eligible` excludes conflicting claims from
  feature-level evidence, including `candidate_omission`; `admit` applies that
  eligibility check to every finding. Yet `source_omission.validate` requires the
  exact conflicting claim set for a `reconciliation_conflict` loss location.
  Source-only evidence cannot satisfy that join. Original source is available,
  but a correct feature-level diagnosis can still be inadmissible. D3 below
  addresses this gap without making conflicting claims valid positive provenance.
- **The review contract has an initial schema/native mismatch.**
  `support.schema.json` makes `question` optional for every initial/insertion
  finding. Native `questionRequired` requires it for three negative decisions and
  forbids it for others. Selected repairs already choose the correct narrower shape.
  The mismatch explains why nine schema-admitted findings required native repairs.
- **Text repair cannot reconsider a wrong decision.**
  `specification_support_repair.merge` updates only the authorized text or evidence.
  It cannot replace an existing finding's substantive decision. That is deliberate
  current authority, not an accidentally missing retry.
- **Semantic actionability remains unchecked by mechanical text validation.**
  `validText` establishes bounded, nonblank text; `clarification_preparation` formats
  questions and evidence. Neither proves that the question identifies a real gap.
  The generic repair wording also asks for a missing decision “and expected answer
  format”; observed repairs ask the user to specify the format instead of providing it.
- **The retained run followed its admitted negative findings.**
  `required_authority.resolve` routes `unsupported` to a clarification. Safe
  false-conflict repair requires `candidate_omission` plus a validated producer
  location. This run never supplied that diagnosis.
- **The incomplete renderer exposes the consequence.**
  It intentionally emits only eligible business/scope claims. Making it print
  conflicting claims as supported would bypass the evidence contract.

The existing prompts already say to retain compatible overlap, distinguish kinds
from conflicts, review source meaning, and challenge false conflicts. Adding the
same warnings again is not an evidence-based remedy.

### What this analysis does not establish

- It does not establish why the model internally chose a false conflict, that
  temperature caused it, or that a larger model/reasoning setting will fix it.
- It does not establish context clipping: the inspected requests retain the source
  and relevant claims. High token use alone is not clipping evidence.
- It does not show a missing message stack, broken deterministic JSON assembly,
  token-budget exhaustion or lost source bytes.
- It does not show that the principle assessment failed. That assessment was not
  reached. Principle capture/configuration is distinct from assessment execution.
- It does not prove that an extra review will be correct. Semantic improvement
  requires controlled measurement and the complete live path.

The existing false-conflict regression in
[`specification_generation_test.zig`](../src/specification_generation_test.zig)
supplies a conflict-subject `candidate_omission` and `reconciliation_conflict` beside
supported findings. It proves that selected repair case; it does not cover
feature-level loss attribution, mixed findings or live recognition accuracy.

**Additional routing gaps confirmed by code inspection:**
[`required_authority.reconcile`](../src/domain/required_authority.zig) gives
`needs_user` precedence over candidate defects, and `supportedOmission` requires
the overall result to be `invalid`. Separately,
[`source_omission.select`](../src/domain/source_omission.zig) rejects the first
unlocalized omission before considering a later localized one. Thus a valid repair
can be bypassed by another finding. These are recovery-path gaps, not claimed events
of the retained run, which never supplied a localized omission. D4 now tracks the
concrete §12.8.1 routing requirement; implementation and recovery tests remain open.

### Final design review — remaining implementation risks

| Gap or brittleness | Required disposition |
| --- | --- |
| Broad unsupported/uncertain → clarification wording contradicted source-loss repair | Replaced by the single §12.8.1 admission/precedence contract; no caller-specific branch |
| Completing a question could make an unresolved interpretation look like a user gap | Gate the whole ledger before need construction; no generic descriptor fallback for an inconclusive semantic finding |
| Genuine source conflicts and false candidate conflicts share record shapes | Require current exact-subject assessment before classification; preserve genuine choices and reject unclassified candidate conflicts |
| Review wire shape lacks an explicit inconclusive result | Settle the smallest closed rejection representation at its canonical owner before implementation; never require a made-up question to satisfy the schema |
| D1 was presented ahead of the semantic gate | Treat it as a separate measured schema change; it does not establish question necessity or preserve initial-response siblings by atomic merge |
| Pending forms could be mistaken for prior proof | Validate bindings on readback; rebuild current semantic evidence on fresh execution, without a parallel evidence store |

These are documentation corrections and implementation acceptance requirements.
The native gate still needs implementation. Model-assisted absence/conflict judgments
can remain wrong even with valid evidence bindings; live tests must measure both
unnecessary questions and missed genuine decisions. Fewer questions caused by more
terminal failures do not close specification-generation readiness.

## 4. Owners and complete change surface

| Responsibility | Existing owner | Required boundary |
| --- | --- | --- |
| Source extraction and reconciliation proposals | `reference_extraction*`, `reference_reconciliation*`; configured prompts/compositions | Preserve captured meaning; classification is candidate data. |
| Review assignments, applicability and packet | `specification_authority.zig`, `specification_support.zig` | One native requirement ledger; sources remain available for diagnosing loss. |
| Evidence/text/question rules | `specification_support_evidence.zig` | One rule projection for initial requests, insertion, repair, admission and readback. |
| Model-facing shape | `design/workflows/spec/support.schema.json`, existing schema compiler/projection and `model_candidate_json.zig` | One canonical schema resource; no parallel provider or repair schema authority. |
| Review repair | `specification_support_repair.zig` and existing actions/bindings | Selected target, revision, provenance, sibling preservation and complete revalidation. |
| Source-loss repair | `source_omission.zig`, extraction/reconciliation `Omission` owners | Unique producer and smallest authorized target; no invented source precedence. |
| Continuation | `required_authority.zig`, compiled workflow transitions | Domain facts do not create local success/clarification rules. |
| Execution/dependencies/accounting | `workflow_pipeline_runner.zig`, `workflow_retry.zig`, model lifecycle owners | One runner; bounded recurrence and global actual-token enforcement. |
| Questions and forms | `required_authority_clarifications.zig`, generation need builder, `clarification_preparation.zig`, refresh/views | Presentation cannot diagnose, answer or repair semantics. |
| Policy assessment | `principle_assessment.zig`, `specification_review.zig` | Source support and principle consistency retain distinct purposes. |
| Persistence/publication | `reference_snapshot.zig`, `specification_state.zig`, incomplete-spec and workflow-output owners | Fresh contract readback, current evidence, protected forms and validated output. |

Shared review code serves both source and principle findings. Source changes must
not impose business questions on principle findings or duplicate their citation rules.
The generation clarification variant is a sibling producer of the same form
preparation contract and must be covered whenever question preparation changes.

## 5. Decisions that must remain explicit

The 22 September corrective design defines clarification admission, diagnostic
eligibility and mixed-outcome precedence in §12.8.1. D3/D4 below now track that
documented contract, not open-ended routing choices. D1's wire representation and
correction scope were subsequently approved on 22 September; optional D2
reassessment remains unapproved. Neither approval relaxes verdict authority.

### D1 — Initial/insertion finding shape

**Approved by the user on 22 September 2026; implemented and verified offline (§11).**
Approval covers both the response contract and correction-scope trade-off.

The focused [§12.7 amendment](../design/contracts/12-model-boundary.md#127-workflow-defined-model-operations)
uses the existing compiler's `oneOf`/`kind` convention. Source findings have one
`kind` discriminator. `ambiguous`, `conflicting` and `unsupported` require `detail`
and `question`; supported, applicability, candidate-loss and inconclusive variants
forbid `question`. Shared fields remain canonical schema references. The existing
native source candidate carries `kind` and retains optional question data for
precise defensive validation; the shared accessor preserves principle reviews'
separate `decision` contract. The old source `decision` format is rejected without
an alias or dual reader. Canonical persisted evidence is unchanged by D1.

**Recovery trade-off:** a missing/forbidden question in an initial review now rejects
before native collection. Existing protocol correction resubmits the complete
assigned review under its original assignment allowance, including otherwise valid
siblings. It does not retain them by atomic merge. Insertion corrects only its
selected finding; native text/evidence repairs retain their existing narrow scope
and independent defect allowances. No partial parser, sibling-merging correction
path, new counter or verdict reassessment is introduced. Complete corrected reviews
still undergo assignment, evidence and domain validation.

This change removes the optional-everywhere source wire schema; it does not remove
native decision-dependent validation or prove that a negative finding is true.
Regression coverage must exercise changed/dropped siblings, errors moving between
findings, bounded recovery/exhaustion and exact token charging. Record the expanded
schema/request cost. D2 remains unapproved; Phase 4 semantic necessity remains open.

### Focused prerequisite: inconclusive result (approved 22 September 2026)

Add `inconclusive` to the existing source-review decision and native finding
contracts. Require a nonempty diagnostic `detail`, prohibit `question`, and retain
current evidence validation. It denotes an unresolved interpretation defect, with
no candidate-repair authority of its own. Retain it in the complete ledger while
other eligible producer repairs run; if still unresolved after rebuilding and
review, terminate without new specification or clarification publication.

Give the existing specification-generation response union the corresponding
`inconclusive` alternative with a nonempty `detail`. It follows the existing invalid
result path, rather than becoming a clarification or repairable content field.
Apply the closed variants to canonical schemas, codecs, request guidance, consumers
and persisted validation together. Reject superseded persisted contracts if their
versioned representation changes; add no compatibility reader.

This is solely an explicit failure representation. It does not approve D1's broader
question-schema redesign, D2 verdict reassessment, a new reviewer, or extra retries.
Existing negative findings must still identify an actual missing decision; native
checks cannot prove the truth of that semantic assessment.

### D2 — Reassessment of a structurally admitted source finding

**Not approved. The earlier explicit user decision retained verdict authority.**
See [R44](IMP_001.md#r44-delivery--selected-schema-fidelity-and-omission-diagnostics)
and [§22.1](../design/contracts/22-repair.md#221-definition-of-atomic).

The default rollout preserves that decision. Initial task/evidence improvements
and measurement can proceed without introducing verdict rewriting. If an admitted
false negative must be reconsidered automatically, obtain a new explicit amendment;
do not disguise it as question repair, JSON correction or formatting.

The smallest proposal to evaluate is a bounded reassessment of a selected source
finding that proposes a new human clarification, through the existing review and
runner owners:

- Use that typed outcome as the trigger across source domains, not wording,
  fixture IDs, model names or a native guess that the model is wrong.
- Bind the original assignment, current source/candidate dependencies, original
  finding and origin; retain the earlier response as historical evidence.
- Permit an initial proposed maximum of one semantic reassessment per stable
  finding/dependency scope, subject to approval. Configure it through existing
  runner-owned limits; retain existing protocol-correction bounds and token charging.
- Accept any justified existing result, including a genuine negative. Do not vote,
  repeat until positive, infer progress from different wording, or reset the limit
  when the decision alternates, an unrelated target is repaired, or derived IDs and
  revisions are renewed. Freshness bindings must not become a retry-reset mechanism.
- A candidate-loss result still needs a unique authorized producer and rebuilding.
  A positive finding cannot clear a forced candidate defect or bless conflicting claims.
- Inconclusive reassessment without an identifiable decision follows §12.8.1's
  terminal rejection; it cannot fabricate a question, completion or extra allowance.
- Amend §12.7/§16.4/§17/§22.1 together as applicable, including exact admission,
  exhaustion and freshness rules, before implementing the additional authority.

This is a decision proposal, not a selected new mechanism or a promise of success.
If declined, retain the current boundary and report any remaining live semantic
failure honestly. An approved comparison of configured model profiles is another
experiment; it is not an automatic fallback or established solution.

### D3 — Diagnostic evidence admission

**Implemented and verified offline under §12.8.1; see §9.**

Define a legal representation for feature/candidate loss attributed to an existing
producer whose claims are ineligible for positive content. Reuse
[`specification_support_evidence`](../src/domain/specification_support_evidence.zig)
for evidence rules and `source_omission` for producer/location joins. Diagnostic
eligibility must agree with those joins; source-only evidence is permitted only
where the selected loss contract accepts it. Keep positive provenance unchanged.

Apply that single rule to initial review, insertion, repair guidance/schema selection,
collection and persisted validation. Packet construction projects validator-owned
choices; it must not select citations for the model or create another evidence
policy. Cover cases where every business claim is classified as conflicting, plus
unrelated source domains and wrong/stale targets.

### D4 — Mixed findings and repair selection

**Implemented and verified offline under §12.8.1; see §9.**

Implement the canonical precedence table rather than repeating it in YAML: terminal
failures and authority blocks retain priority; eligible authorized repairs precede
clarification publication; unresolved defects with no eligible repair terminate.
Retain unlocalized findings while selecting later localized targets. Genuine gaps
remain in the ledger and reach publication only after candidate/review validity.

Keep continuation in `required_authority`, producer selection in `source_omission`,
and execution/rebuilding in the existing runner and compiled transitions. Reconcile
the candidate-conflict assertion with exact-subject source evidence under §12.8.1;
an unrelated `unsupported` finding cannot discharge it. Every accepted repair rebuilds
affected dependents and reassesses all findings. Genuine gaps remain unresolved until
their authority is supplied; source/target ambiguity never grants repair authority.

## 6. Phased rollout

### Phase 0 — Capture the complete regression and baseline

**Can proceed under existing authority.**

1. Preserve the minimal failing input/response sequence in existing test fixtures:
   compatible overlap across content kinds, false dispositions/conflict, nine
   unsupported findings without questions, text repairs, and the clarification exit.
   Keep raw malformed-prefix evidence separate from admitted normalized candidates.
2. Add unrelated representatives, such as loan renewal plus return-date output,
   rather than only changing the greeting text. Include genuinely incompatible
   source requirements and a genuine missing business decision.
3. Exercise real packet construction, compiled schema selection, collection,
   authority routing and renderer preparation using fake narrow ports. Preserve
   current behaviour as diagnostic evidence, not a desired nine-question outcome.
   Include feature-level conflict loss and mixed localized/unlocalized/gap findings
   in different orders; do not give every sibling a supported finding by construction.
4. Record source/schema/prompt identities, request bytes, provider usage and native
   repair counts. Mark all replay tests as offline; none substitutes for live E2E.

**Exit:** reproducible evidence distinguishes protocol failure, malformed review,
candidate loss and a real source gap. Changed expectations identify the applicable
§5 decision; no permanently skipped test or weakened assertion is introduced.

### Phase 1 — Align initial review schemas with native question rules

**Depends on Phase 0 and D1 approval.**

1. Implement the approved finding representation in the canonical support schema
   and existing native/model-codec boundary. Share common definitions rather than
   copying full subtrees for every decision.
2. Apply it to initial source review and missing-finding insertion together.
   Check source and candidate-support review, applicability, protocol correction,
   selected text/evidence repairs, debugger inspection and stored evidence admission.
3. Keep native text, evidence, exact-set, source freshness and loss-location checks.
   Schema success never authorizes semantic support or selects evidence.
4. Remove obsolete optional-everywhere source shapes and superseded tests/readers.
   Preserve the distinct no-question principle contract.

**Exit:** every user-gap variant missing `question` rejects at schema admission;
inconclusive outcomes cannot require a fabricated question. Selected native
repairs preserve siblings; whole-review corrections exercise D1's scope trade-off,
full validation and exact assignment accounting, including errors moving between
findings. Demonstrate actual compiled request shapes and measure repair traffic
and content drift; do not claim schema conformance resolves false conflicts.

### Phase 2 — Make the existing source review assess the right evidence

**Depends on the baseline and D3 for admission changes; does not require D2.**

1. Implement the shared §12.8.1 gate for both source-review and generation-created
   needs. Require a current necessary decision, keep inconclusive review typed as
   failure, and remove generic semantic-gap fallback questions. Preserve legitimate
   mechanically established questions and existing subject/answer authority.
2. Apply D3 through the shared evidence and loss owners for every subject: feature,
   signal, conflict, token, entity applicability and candidate field. Align request
   guidance with admission/readback while preserving positive-output provenance.
3. Derive displayed legal loss targets/evidence choices from those same rules.
   Prove feature-level conflict attribution can pass both evidence admission and
   loss validation; source-only evidence must not bypass exact conflict joins.
   Do not ask the model to invent an owner, ID or permitted repair scope.
4. Simplify current task/guidance presentation so source sufficiency is assessed
   from meaning, not the presence of prewritten title/story/AC/FR fields. Retain
   original source once, derived claims and the relationship under review. Replace
   redundant prose; do not add another summary call or duplicate policy assessment.
5. Preserve legitimate `not_applicable` choices under existing native rules.
   Absence of business entities is not automatically a missing user decision, and
   absence of a timestamp format does not authorize inventing one.
6. Test the extraction contributor: business behaviour repeated as technical
   content must not imply conflict. Keep classification semantic; no lexical
   deduplicator, universal “overlap is compatible” rule or change to precedence.
   In the existing extraction guidance, prefer independently meaningful claims
   while preserving coupled conditions. Test grouped and separate representations;
   do not add a sentence splitter, require one claim per source line, or prescribe
   a fixture-specific claim count. Measure the effect rather than promising it.

**Exit:** correct diagnoses are representable and admissible without weakening
positive provenance; a known inconclusive review cannot become a user gap. Packet,
repair and readback tests enforce the same rule and reject unsafe attribution.
Live recognition remains a Phase 6 measurement; prompt wording alone is not proof.

### Phase 3 — Complete shared routing and candidate repair

**Depends on D3/D4 for the revised recovery route; D2 remains separately optional.**

1. Implement D4's approved mixed-outcome and target-selection contract through its
   existing owners. Cover localized defects beside genuine gaps and unlocalized
   defects, including different orders and blocking outcomes. Retain every finding;
   never convert an unsafe candidate defect into a user question or success.
2. Prove the existing coupled repair when review supplies
   `candidate_omission` with `reconciliation_conflict`: authorize the exact coupled
   disposition/conflict group, merge with old-value/revision checks, rebuild missing
   signals and coverage, and rerun current support review.
3. Cover both pre-generation and post-generation discovery. Invalidate dependent
   content/reviews/assessments through declared action dependencies and the runner.
   Do not reuse successful slices whose dependencies changed; retain unrelated
   siblings only while their exact dependency bindings remain current.
4. If D2 is approved, implement its selected reassessment through the existing
   packet, admission, repair/evidence and runner owners. Amend the existing selected
   finding operation only as authorized; do not add a second review authority,
   hidden model invocation or unbounded successor loop.
5. If D2 is not approved, keep admitted verdicts immutable and leave recovery from
   those false negatives explicitly unresolved. Known inconclusive findings terminate
   under §12.8.1; completing question text never authorizes a fabricated user gap.

**Exit:** every mixed case follows D4's approved outcome/selection contract. An
authorized repair rebuilds all affected dependencies before reassessment; unsafe
or stale attribution grants no repair, and unresolved defects/gaps retain their
required outcome. If enabled, reassessment exhausts its approved allowance while
resolved independent work can continue within the global token budget.

### Phase 4 — Complete actionable questions and downstream integration

**Depends on the selected review/routing contracts; preserve publication authority.**
The latest retained run isolates optional applicability without a reconciliation
conflict; apply the concrete follow-up and regression distinctions in §10.

1. Use existing fields: `detail`/`why_required` for known facts, preconditions and
   why a choice is needed; `question` for the precise choice and expected answer.
   The request must provide the answer format, not ask the user to design it.
2. Apply that contract to initial findings, insertion, selected question repairs
   and generation-created needs. Give field-specific diagnostics for mechanically
   missing/invalid content. Do not claim a nonempty-string check proves actionability.
3. Keep formatting pure. It may select existing cited excerpts, remove contained
   duplicate quotations and reference the full sidecar when needed. It must not
   infer a gap, invent an answer, rewrite a verdict or combine unrelated subjects.
4. A concern that identifies no missing decision stays at the review boundary;
   use §12.8.1's terminal rejection if no authorized correction is available. Do not
   make this depend on optional D2 or produce a generic “describe this requirement”
   form. Semantic assessment remains model-assisted.
5. Verify complete generation and principle assessment after source support recovers.
   The current principles include greeting-only restrictions while the story asks
   for UTC date/time. Under ADR 0015, preserve clear intent and record the cited
   policy conflict as a mandatory Plan obligation. Do not silently waive principles,
   remove the UTC requirement or create premature Plan clarifications.
6. Preserve both output routes: complete supported AC/FR content renders through
   the existing specification renderer; genuine gaps render the validated incomplete
   projection plus actionable forms. Neither route bypasses unrepaired validation.
7. Validate completed-state source review/principle evidence and exact extraction
   contract on readback. Pending state currently stores references and clarification
   bindings, not a complete source-review transcript: test its contract and fresh-run
   rebuilding. No extra gap certificate/store or interpretation of historical prose
   as current proof is needed. Cite §12.8.1 instead of creating a second readback rule.
   Preserve user-closed forms and stable subject identities.

**Exit:** users are asked for a decision the supplied source does not already answer.
Genuine gaps retain current facts, a concrete question and a usable answer format.
Policy conflicts and business gaps remain distinct. No renderer-only semantic fix.

For a genuine gap, the [§12.7 output example](../design/contracts/12-model-boundary.md#127-workflow-defined-model-operations)
shows preconditions, question and expected answer using existing fields. Reports
must distinguish `awaiting_clarification` with form IDs/paths and validated incomplete
publication from an error with no new publication. Completed-spec grading is
`not_run` in both cases; the error must not ask the user to supply an engine diagnosis.

### Phase 5 — Full conformance, resource measurements and packaging

**Depends on all selected implementation phases.**

Extend existing tests at their owning boundaries; use the matrix below. Compare
fixed-input requests before/after at the complete schema, selected schema, guidance,
input and correction levels. Measure UTF-8 bytes separately from actual provider
tokens. Report call counts and retained evidence size; do not invent token estimates.

Compile the actual shipped workflow and record its expanded graph/resource use.
The current finite graph limit is 1,024; this rollout does not propose raising it.
If extra work is necessary, measure and simplify existing composition before
requesting another capacity decision. Preserve independent request identities.

Repository-owned checks, selected according to changed scope, then full verification:

```sh
zig build test-model-result-schema test-model-candidate-json
zig build test-reference-extraction test-reference-reconciliation test-reference-model-input
zig build test-specification-generation test-required-authority test-clarification-inputs
zig build test-specification-contract test-model-request-preparation test-model-request-workflow
zig build test-workflow-repair-retry test-model-attempt-accounting test-e2e-harness
zig build verify
git diff --check
```

`verify` includes the repository's native `smoke` packaging check. Run the same
standalone `zig build smoke` step during iteration when packaging/runtime boundaries
change; a source-tree run is not packaging evidence. Do not run redundant full
suites without a change, failure or unresolved concern that warrants it.

**Exit:** required tests, architecture/schema checks and clean-environment packaging
pass; request/graph measurements are recorded; the diff contains no duplicate
policy, compatibility path, provider-specific semantic workaround or weakened test.
Offline completion does not close live semantic readiness.

### Phase 6 — Controlled live assessment and closure

**Each live invocation requires separate explicit approval.**

1. Present the exact cases, workflow, target projects, model/configuration, budgets
   and evaluation scope after Phase 5. Use the existing harness and evidence capture.
   Do not reuse a past one-run approval or rerun automatically until success.
2. Assess source preservation and question necessity on the motivating case and
   unrelated complete, incomplete and contradictory sources. Controlled request
   experiments are not complete-workflow E2E evidence; label them accordingly.
3. Record every approved attempt, including failures: supported requirements
   retained, false conflicts, unnecessary questions, correct candidate-loss routing,
   genuine-gap actionability, protocol/native repairs, tokens and terminal outcome.
   Do not infer a reliability percentage from one successful run.
4. Complete an approved publication-and-scoring run using the existing command:

   ```sh
   ./scripts/e2e-spec.sh --case test/e2e/wf-001-hello-world/node-vitest/workflow.case.json
   ```

   The currently configured generation and grading budgets are each 100,000 tokens.
   Approval must name the configuration actually used when the run is proposed.
5. Inspect the actual published specification and persisted policy obligations.
   The current grading case supplies `stories.md` and a business rubric; it does
   not independently establish principle-assessment completeness. Native state
   checks plus inspection of cited policy findings must cover that obligation.
6. Report the rubric score as recorded. Its current `pass_threshold_percent` is
   null; do not invent a pass threshold or equate “scored” with “quality passed”.
   Any new quantitative reliability target, trial count or grading-policy change
   must be agreed before the experiment, not selected after seeing its output.

**Exit:** a published, scored specification preserves source meaning without fixture
logic; genuine-gap cases produce useful questions; negative cases still fail safely.
If semantic failures persist, retain the open result and use the captured evidence
to choose the next approved change. Do not close readiness on improved syntax alone.

## 7. Required regression matrix

| Case | Required evidence |
| --- | --- |
| Latest complete failure sequence | False cross-kind conflict → unsupported review → missing questions → text repairs; no claim that formatting repaired the verdict. Changed expectations identify the applicable §5 decision. |
| Successful localized recovery | Conflict diagnosis → coupled repair → rebuilt signals/coverage → current review → generated AC/FR → policy assessment → validated publication. |
| Diagnostic eligibility | Feature/candidate loss with all business claims conflicting; exact producer/claim/source joins; unchanged positive eligibility; initial/insertion/repair/readback agree; wrong or stale targets reject. |
| Mixed findings | Localized plus unlocalized defects, localized defects plus genuine gaps, and all three together in different orders; preserve blocking outcomes, dependent reassessment and D4's explicit terminal policy. |
| Compatible overlap across unrelated domains | Preserve both meanings; no forced conflict, duplication-based precedence or hard-coded expected prose. |
| Genuine contradiction | No automatic deletion, positive verdict or invented precedence; identifiable choice with cited incompatible meanings. |
| Genuine missing decision | Known facts retained; precise question and supplied answer format; incomplete publication remains a normal pause. |
| Inconclusive versus genuine need | Known source facts beside a generic unsupported finding; contradictory current assessments; source-review and generation needs; no semantic-gap fallback question, and failure without publication when no safe correction exists. |
| Schema/native conformance | Every source decision across initial, insertion and selected repair; positive/applicability/omission question prohibition; separate principle variants. |
| D1 correction scope | Missing questions across several findings; repeated/changed errors, dropped/altered siblings, recovery/exhaustion; distinguish complete initial-response correction from selected native repair and verify exact allowance/token charging. |
| Evidence and applicability | Exact claim/source joins, source-only omission where permitted, unavailable evidence, wrong IDs, real entities, no business entities and unrelated principles. |
| Retry/accounting | Many successful targets plus recurring/alternating defects; protocol/native failures; exact input/output usage, exhaustion and no call at/above budget. |
| Provenance and dependency renewal | Unrelated siblings unchanged; changed source/policy/response invalidates dependents; historical stack records cannot regain current authority. |
| Failure before publication | Unrepairable JSON/schema or invalid runner delta with open forms: terminal error, no new spec, no completion or grading. |
| Persistence and fresh rerun | Exact extraction binding; completed review/policy readback; pending-state validation; protected closed forms; no stale answer or saved partial execution reuse. |
| Predecessor gates | Open Spec blocks Plan; open Spec/Plan blocks Tasks; deferring policy obligations does not bypass business clarification gates. |
| Cleanup/logging/packaging | Retained raw/normalized distinction, request/repair/terminal association, no leaked request/stack owners, write/log failure propagation, clean packaged execution. |

## 8. Completion record and immediate next step

- [x] Trace retained execution through source, requests, repairs, authority and output.
- [x] Compare current fixture files with the retained target inputs/resources.
- [x] Identify existing owners, mechanical mismatch, semantic limits and authority decisions.
- [x] Record final-review admission/routing gaps and D1's recovery-scope trade-off.
- [x] Strengthen the canonical design, remove conflicting broad routing wording,
      and specify genuine-gap output plus §28.9 conformance requirements.
- [x] Phase 0: offline failure replay, unrelated sources and request capture.
- [x] Approve and implement the focused inconclusive-result contract.
- [x] Phases 2–3: shared admission, D3 diagnostic eligibility/readback and D4
      repair selection/precedence, including post-generation selection.
- [x] Phase 1 / D1: approve, implement and verify the initial/insertion wire redesign
      and correction scope (§11).
- [x] D2 remains excluded: no verdict reassessment or additional review call.
- [x] Phase 4 implementation: shared applicability/question guidance and §10's
      regression, generation, principle-assessment and readback coverage (§12).
- [ ] Phase 4 live acceptance: question necessity against actual source/principle
      evidence, assessed under Phase 6.
- [x] Full verification, request measurements, native packaging and diff review for this delivery.
- [x] Phase 6: execute and inspect one separately approved live attempt (§13).
- [ ] Phase 6 closure: completed publication, scoring and remaining semantic assessment.

**Next:** address the broader source-sufficiency failure documented by the approved
live attempt (§13). Offline implementation is verified; live acceptance is not met.
D1 is implemented; do not silently enable D2 or automatically rerun.
A false negative with no mechanically established candidate defect can still be
misjudged by the model. This change does not establish live semantic reliability.

## 9. Phase 0 / 2–3 delivery — 22 September 2026

### Implemented contracts

- `required_authority` keeps candidate/interpretation defects ahead of user gaps;
  administrative blocks and upstream rework retain their priority. Contrary current
  judgments about the exact same decision/resolution are an interpretation failure.
  Only exact-subject, current `conflicting` source evidence can discharge a candidate
  conflict assertion. Positive or generic unsupported findings cannot discharge it.
- `specification_support_evidence` derives diagnostic selections through
  `source_omission` and validates the selected producer joins. Localized signal/conflict
  diagnoses use their exact claims even when those claims cannot support content;
  authorized source-only locations remain valid. Positive content eligibility is
  unchanged. Insertion, selected evidence repair, collection and readback share this
  owner. Source evidence retains `loss`; `specification-state/v4` rejects older state.
- `source_omission` skips an earlier unlocalized diagnosis to find a later eligible
  target. The existing specification omission owner also skips unsafe content targets.
  Neither removes unresolved findings. The runner's existing renewal/invalidation
  path rebuilds dependencies and reviews the complete ledger after a producer repair.
- Source reviews and generation can return `inconclusive` with diagnostic detail
  and no question. This grants no repair authority of its own. Source findings remain
  immutable under text repair; unresolved interpretation terminates without new output.
  Question construction rejects unresolved candidate defects and no longer supplies
  descriptor questions for semantic concerns lacking an admitted question.
- Post-generation failure to authorize any safe omission repair now returns `invalid`,
  with matching declared YAML transitions. No new action, orchestrator, retry counter,
  evidence store or model call was introduced. The compiled graph remains 590 operations
  within the existing 1,024 limit. Extraction guidance prefers independent meanings
  while retaining coupled conditions; it does not impose sentence splitting.

### Regression evidence

The offline production-path replay injects a false cross-kind conflict, nine
unsupported findings without questions, and nine successful text repairs. It makes
17 fake-provider calls, retains the unsupported verdicts and ends invalid with no
new specification, forms or completed-spec authority. Its synthetic usage is exactly
85 input + 34 output = 119 tokens; this checks accounting, not model tokenization.

The existing recovery replay repairs the conflict group, rebuilds signals/coverage,
reruns review, generates AC/FR, assesses policy, publishes through the normal writer
and validates persisted readback. Existing unchanged-conflict cases exhaust the
original allowance. New initial/post-generation inconclusive cases assert zero
semantic repair calls, exact accounting and no publication.

Owning-boundary tests cover unrelated renewal/date requirements, real missing
choices and conflicts, mixed localized/unlocalized/gap findings in different orders,
post-generation target selection, unchanged siblings/origins, diagnostic insertion
and selection repair, corrupt loss/source/positive-evidence readback, and conflicting
current assessments. Existing freshness, renewal, protocol-exhaustion, global-token,
publication-failure, protected-form and policy tests remain in full verification.
These are offline tests with fake provider data, not live model or E2E evidence.

### Request measurements

Captured replay requests: `.zig-cache/fix002-question-conflict-<call>-<attempt>.json`.
Each includes request content, selected schema and serialized Bedrock request.

| Request component | Before | After | Basis |
| --- | ---: | ---: | --- |
| Source review content | 6,679 bytes | 7,004 bytes | Same captured input, substituting the predecessor prompt and evidence instruction |
| Selected review schema | 2,760 bytes | 2,775 bytes | Same selected schema; addition of `inconclusive` |
| Nine selected question repairs, content | — | 34,112 bytes total | Captured requests, including input and guidance |
| Nine selected question repair schemas | — | 1,782 bytes total | Existing selected `gap_detail` schema |

The before values are a fixed-input projection comparison, not execution of an old
binary. The request change is explicit failure/diagnostic guidance, not an asserted
size reduction. UTF-8 bytes are not provider tokens. Actual model token use and
semantic effectiveness require the separately approved live assessment.

### Verification

- `zig build test-specification-generation test-required-authority --summary failures` passed.
- Final `zig build verify --summary all` passed **128/128 steps and 1,714/1,714 tests**,
  including native packaging smoke checks. Log: `.zig-cache/fix002-verify-final.log`.
- `git diff --check` passed; no live invocation was performed.

## 10. Latest retained run — optional applicability becomes a user question

**Reviewed 22 September 2026; documentation only.** This review reads an existing
run; it does not execute or authorize another invocation. §9's offline verification
remains valid for its stated scope, not proof of semantic question necessity.

### Observed sequence

Run [`2026-09-21T22-27-26Z-667084464df223d53775282ad8a744cf`](../zig-out/e2e-spec/2026-09-21T22-27-26Z-667084464df223d53775282ad8a744cf/report.json)
ended `awaiting_clarification` / `needs_user`, published an incomplete specification
and S01, and did not run grading. This is a failure to reach the completed/scored
goal, not a terminal engine or unrepaired JSON error.

- Nine model calls used **16,489 input + 2,088 output = 18,577 / 100,000 tokens**;
  accounting was complete. All attempts were attempt 1. One native review-text
  repair ran; no protocol correction or retry exhaustion occurred.
- Seven `REMOVED_LEADING_BRACE_QUOTE` events appear in the feature event log.
  Raw provider text therefore was not uniformly valid JSON. The admitted responses
  passed after existing normalization; harness events contain no JSON/schema error.
  Do not describe this as seven model repairs or as provider syntax reliability.
- Extraction retained startup, greeting and UTC output in one business claim plus
  the exact-string claim. Reconciliation retained both, produced two signals and
  **no conflicts**. The prior false-conflict sequence is not this run's cause.
- Call 8 returned ten `supported` findings and one `unsupported` finding for
  ordinal 10, entity applicability. Its explanation was: “No claim indicates the
  presence or absence of business entities or data; insufficient evidence to
  determine.” It cited source 1, no claims, and omitted `question`.
- Event 215 reported `missing_question`. Call 9 was authorized to replace only
  `detail`/`question`, explicitly preserving the retained `unsupported` decision.
  It added: “Does the application involve any business entities or data? Please
  answer \"Yes\" or \"No\".” Merge passed at event 232; authority reconciliation
  and gate returned `needs_user` at events 237–238; publication followed at 247.
- S01 repeats the review uncertainty as its reason. It names no source-required
  behavior that depends on the answer; “Yes” would still leave the entities and
  necessary behavior unspecified. The incomplete view retains all three behaviors
  in a joined sentence. Full AC/FR generation was never reached.
- Principles were captured at event 23, but no principle-assessment operation/model
  call ran. The source-review packet contained reference evidence, not principle
  bodies. Capture is not assessment. The greeting-only policy versus UTC requirement
  remains a later policy obligation; it did not cause this pause.

Evidence: retained calls 1, 5–9, `events.jsonl`, feature event log, `S01.md`, `spec.md`
and `specification-state/v4` pending snapshot. At this pre-D1 review, all **29**
case-mapped resource files matched the then-current repository sources byte-for-byte.
The subsequent D1 delivery changes the source schema; this run does not test it.
Call 8 already contained the
approved `inconclusive` alternative and its guidance, and the entity task allowed
`not_applicable: no_business_data`. Neither option was missing from the request.
The executed modified build records revision `2e612726607576c967b507806ab601460c216f7d`
and source digest `83d68b4a3892cdea29764e834573de6bbc8417cca4881fc24fc17150c7845d77`;
resource equality alone does not prove equality of the whole native build.

### Owning gap and limits

The remaining issue is the semantic choice between supported non-applicability,
an actual missing business decision and unresolved interpretation. The shared task
currently says only “Whether business entities/data are involved.” The enum rule
name does not explain that distinction. The source describes concrete output
behavior; the finding identifies no necessary additional business-data decision.
That makes the question unjustified by the retained evidence, rather than proving
that every silent source necessarily supports `not_applicable`.

Current native admission checks registered applicability, evidence joins, shape,
text and known candidate defects. It cannot establish semantic necessity from a
nonempty explanation. The new terminal handling applies when the model reports
`inconclusive` or a candidate defect is mechanically established; it does not
reinterpret arbitrary `unsupported` prose. Other supported findings concern
different subjects, so shared citations do not create a same-subject contradiction.
Text repair correctly preserved its authority boundary, but filling in a question
did not make the diagnosis correct. D1 could avoid this missing-question repair;
it does not prove that a fully shaped negative finding is justified.

### Required follow-up within Phase 4

1. Extend the existing regression owners with this **no-conflict** sequence:
   supported source behavior → optional applicability marked unsupported → missing
   question → successful text repair → unnecessary pause. Record the present semantic
   limitation honestly; a scripted corrected verdict is not evidence the model will
   select it. Preserve siblings, provenance and exact retry/token accounting.
2. Clarify the existing requirement-description and source-review guidance owners,
   replacing terse/redundant wording. Assess whether the specified behavior requires
   business entities, not whether the source contains an explicit entity heading or
   declaration of absence. Explain permitted non-applicability using source meaning
   and required supporting claims. Displayed values alone do not establish a managed
   business entity; no automatic default, citation selection or fixture exception.
3. Keep three outcomes distinct in requests and acceptance evidence: evidence-backed
   non-applicability; a concrete missing decision with its effect on required behavior;
   and `inconclusive` with no question. Reuse existing fields, policy, collection,
   repair and readback owners. Text repairs cannot change the retained verdict.
   An expanded semantic reassessment or different wire contract still requires its
   D2 decision or further D1 amendment; do not hide either in guidance or the renderer.
4. Cover unrelated output-only requirements, actual business entities, a genuine
   missing data decision and unresolved interpretation. Exercise initial/insertion
   requests, selected repair, admission, generation, publication and readback where
   applicable. For admitted questions, existing `detail` must connect known facts
   and missing choice to the required outcome; the expected answer must resolve it.
   Merely testing nonblank text or the `inconclusive` enum is insufficient acceptance.
5. Measure the changed requests, run scoped/full verification for implementation,
   then separately approve live assessment. Successful source review must reach
   AC/FR generation and principle assessment; source intent must survive policy
   conflicts. Do not claim outcome improvement from one run or from fewer questions.

No new authority amendment is established by this evidence: §§12.8.1 and 17 already
require necessary user decisions and permit entities only when business data is
involved. Keep D1/D2 separate, retain Phase 4 as open, and require Phase 6 publication
and scoring before closure. This review changes documentation only; no new tests,
model calls, prompt edits or runtime changes were performed.

## 11. Approved D1 delivery — 22 September 2026

**Implemented and verified offline.** The user approved the
decision-specific source shapes and whole-assignment correction trade-off in §5.
The governing amendment is §12.7; D2 and Phase 4 semantic work remain separate.

### Change and regression coverage

- `support.schema.json` replaces the flat source `decision` shape with seven
  `kind` variants. Shared field definitions remain in that schema. Ordinary review
  and insertion exclude `not_applicable`; applicability-enabled selections include
  it. Native per-subject policy still determines where it is permitted.
- `specification_support.zig` and its existing repair owner share one decision
  accessor. Source decoding, insertion, selected repair guidance and reconstructed
  readback use `kind`; principle responses retain their distinct contract. Native
  evidence and snapshots require no D1 format change. The old source format rejects.
- Existing schema/protocol owners reject missing/forbidden questions before collection.
  Whole-review correction retains the original assignment and allowance. Native
  collection still rejects missing assignments and invalid evidence after a syntactically
  successful correction. No new runner path, retry counter, parser or scheduled model operation.
- Schema tests cover every source variant across initial and inserted selections,
  required/forbidden questions and legacy-field rejection. Existing native tests
  retain decision, provenance, sibling and readback checks. Whitespace questions
  exercise the native text boundary; absent questions exercise protocol correction.
- Production-path fake-provider regressions cover repeated/moving question errors,
  recovery/exhaustion, a correction dropping a finding and one corrupting a sibling's
  source ID. Native insertion/evidence repair revalidates the full review, retaining
  unaffected findings and their accepted origin. Exhaustion publishes nothing.
  Every attempt is charged once against the unchanged token owner.
- The historical false-conflict/missing-question replay now exhausts protocol correction
  before native collection, with zero question merges and no publication. This replaces
  the superseded nine-text-repair runtime expectation; it does not prove semantic
  recognition of a fully shaped false finding.

### Measured cost

For the selected initial applicability-review schema, UTF-8 bytes increase from
**2,775 to 16,569**. Explicit variants expand shared references through the existing
compiler. Prompt text and workflow operations are unchanged by D1. Do not describe
this as context reduction or infer provider-token counts from bytes.

The fixed-input fake-provider request has 11,493 content bytes. Replacing only its
schema with the retained pre-D1 selection gives **15,842 → 31,752** serialized Bedrock
request bytes. This fixture disables native structured-output transport; the figure
is not a live request measurement or the size of production's additional native schema
field. Capture: `.zig-cache/d1-review-9-1.json`; method/results:
`.zig-cache/d1-measurements.json`. No live effectiveness or token saving is claimed.

### Verification

- Targeted schema, correction and offline harness checks passed:
  `zig build test-model-candidate-json test-model-request-workflow test-e2e-harness --summary failures`.
- Full `zig build verify --summary all` passed **128/128 steps and 1,715/1,715 tests**,
  including native packaging smoke checks; log `.zig-cache/d1-verify.log`.
- `git diff --check` passed. The final review found no additional production owner,
  compatibility reader, duplicated continuation rule or change to global accounting.
- No live run was performed. Phase 4 acceptance and Phase 6 publication/scoring
  remain open; D1 does not establish that a clarification is necessary.


## 12. Phase 4 delivery — 22 September 2026

### Shared guidance and presentation

- `required_authority_description` explains entity applicability from required
  behavior. Initial review and finding insertion reuse that task; neither needs
  an explicit declaration that entities are absent. Remove that request from the
  existing mechanical descriptor question as well.
- Existing source-review/generation prompts and selected-text guidance require
  known facts/preconditions, the missing choice, its behavioral effect and an
  answer format that resolves it. Necessary details must accompany a yes/no
  choice. Non-applicability retains its claim-evidence rule; inconclusive review
  retains diagnostic detail, no question and terminal failure.
- `clarification_preparation` still only projects supplied text and cited evidence.
  Its evidence heading no longer frames a user decision as an unresolved review
  concern. Quotation containment/deduplication, bounds and sidecar fallback remain.
  No new question fields, authority owner, retry path or workflow operation.

### Regression scope and limits

The existing compiled-workflow regression adds six outcomes in each of two domains:
output-only behavior, actual business records, a missing data decision, native
question-text recovery, inconclusive review and the retained unjustified question.
Initial/insertion packet tests exercise the same applicability description;
selected-text requests retain known facts and behavioral-effect guidance.

- The retained failure is adapted to D1: a missing question fails schema admission;
  whole-assignment correction adds the unjustified question; native admission then
  permits the pause. Other findings, source requirements and exact accounting are
  retained. **This is a characterization of the semantic limitation, not a passing
  claim that the engine recognizes and repairs an arbitrary false negative.**
- Evidence-backed support reaches AC/FR generation and complete publication with
  validated readback. Output-only requirements do not create entity questions.
  A conflicting output-only principle is assessed and retained as a cited Plan
  obligation; it does not erase the additional business requirement.
- Genuine gaps publish pending state and one controlled question containing facts,
  a precise choice, impact and a useful answer format. The user answer stays empty.
  Pending readback binds the form state; unrelated supported findings retain their
  origin. Native text repair preserves the verdict, evidence and siblings.
- Inconclusive review creates neither new specification nor clarification output.
  Existing full-suite tests retain freshness, exhausted-retry, protected-form,
  cleanup, full-validation and global-accounting coverage.

These are fake-provider conformance tests. Guidance cannot deterministically prove
semantic necessity, and no native keyword classifier or D2 reassessment has been
added. Phase 4's live acceptance remains open until Phase 6 inspects actual questions,
source preservation, generated content and principle evidence and scores published
complete output. The separately approved live attempt is recorded in §13; this offline record alone
does not grant live-run approval.

### Validation and measurement

- `zig build test-specification-generation test-required-authority --summary all`:
  **324/324 tests passed** (`.zig-cache/phase4-unit.log`).
- Final `zig build verify --summary all`: **128/128 steps and 1,715/1,715 tests
  passed**, including the offline workflow harness, architecture checks and native
  packaging smoke checks (`.zig-cache/phase4-verify.log`). The existing compiled
  workflow remains **590 operations**. Twelve additional scenario branches extend
  the existing integration test; they do not add twelve top-level test declarations.
- `git diff --check` passed. No schema, workflow graph, validation, retry or token
  policy changed. A regression assertion was corrected to compare Markdown through
  the existing escaping owner while checking retained source bytes independently.

Fixed-input request sizes, in UTF-8 bytes:

| Request | Content before → after | Selected schema, unchanged | Serialized request before → after |
| --- | ---: | ---: | ---: |
| Initial applicability review | 5,109 → 5,305 | 16,569 | 24,842 → 25,039 |
| Whole-review question correction | 8,149 → 8,345 | 16,569 | 28,538 → 28,735 |
| Initial brief generation | 1,965 → 2,027 | 7,723 | 11,257 → 11,320 |
| Selected entity-question text repair | 2,421 → 2,644 | 198 | 3,127 → 3,350 |

Method: capture actual requests from the new cases, then replace only Phase 4's
wording with retained pre-edit wording, holding candidate data and schemas fixed.
The fake-provider transport disables native structured output; these figures do
not measure production's additional native schema field or provider token usage.
Review/generation prompt files grow **52/62 bytes** respectively. There is no
context-reduction claim or extra model request. Captures:
`.zig-cache/phase4-{complete,unjustified,question}-*.json`; method/results:
`.zig-cache/phase4-measure.py` and `.zig-cache/phase4-measurements.json`.

Following offline verification, the user approved one Phase 6 invocation with the
configured Bedrock `openai.gpt-oss-20b-1:0` model (generation reasoning effort `low`)
and generation/grading budgets of 100,000 tokens each. Its result is recorded below;
no automatic rerun occurred.


## 13. Approved Phase 6 attempt — 22 September 2026

The user approved exactly one invocation after Phase 4 verification:

```sh
./scripts/e2e-spec.sh --case test/e2e/wf-001-hello-world/node-vitest/workflow.case.json
```

Evidence: [run report](../zig-out/e2e-spec/2026-09-22T08-29-13Z-3bb59e0d6ddda120e3f4ed99b9677c9a/report.md)
and [source-review response](../zig-out/e2e-spec/2026-09-22T08-29-13Z-3bb59e0d6ddda120e3f4ed99b9677c9a/evidence/generation/call-000008/model_output.txt).
The isolated target remains under that run's `project/` directory. Executed build
source hash: `b0f6f86880c6a569d36a8b80b292b1e815a80e5374e4b6e1afd64f394ab47498`.

**Result:** command exit 0, workflow `needs_user`, report `awaiting_clarification`;
validated incomplete publication passed, completed specification and rubric scoring
`not_run`. Eight calls used **18,981 input + 2,242 output = 21,223/100,000 tokens**.
Every assignment completed on attempt 1; no protocol correction or native repair.
Six responses used the existing logged `REMOVED_LEADING_BRACE_QUOTE` normalization.
There were no subsequent JSON/schema admission failures, exhausted retries or
provider errors. This is a failed semantic acceptance attempt, not a JSON failure
or completed/scored success.

### Causal trace

1. Extraction and reconciliation retained startup, greeting and UTC behavior,
   the exact greeting token, two signals and no conflicts. The source-review
   packet contains the complete original source, including its title, both claims,
   citations, dispositions and signals. The live request contains the updated
   guidance byte-for-byte and the revised entity task; missing request evidence
   does not explain this pause.
2. Call 8 correctly selects evidence-backed `not_applicable` for entities and
   supports the signals/token. It nevertheless marks all seven feature-intent
   subjects `ambiguous`, because it looks for already-written specification fields
   instead of judging whether source meaning supports generating them.
3. Those findings include nonblank questions and valid evidence. Existing schema
   and native rules admit them; no mechanically established candidate defect or
   malformed response triggers repair. Native code cannot infer a wrong verdict
   from arbitrary diagnostic prose, and D2 reassessment remains unapproved.
4. The ordinary authority gate routes the admitted findings to S01–S07 and a
   pending specification. The formatter preserves their text/evidence, deduplicates
   quotations and leaves answers empty; it does not determine question necessity.
5. Principle capture succeeded, but source review stopped execution before full
   AC/FR generation and semantic principle assessment. No policy obligations were
   assessed or scored. Captured principles are not evidence of completed assessment.

### Question necessity review

| Form | Requested information | Source/decision assessment |
| --- | --- | --- |
| S01 | Observable pass/fail outcomes | Successful startup, exact greeting and UTC output already establish observable outcomes. |
| S02 | User-visible description | The source supplies the behavior to summarize; no missing behavior is identified. |
| S03 | Feature name | The original source includes the heading “Hello World Application”. Absence of a dedicated extracted name is not a user decision. |
| S04 | Required application behavior | The source states it directly. |
| S05 | User benefit | The finding identifies no consequential choice beyond the specified behavior; it does not explain why an additional decision is needed. |
| S06 | Actor/action/result | The finding demands an explicit format without identifying an unresolved actor choice that changes required behavior. |
| S07 | Triggers, outcomes, exact copy | Startup and the required outputs, including the exact greeting, are already present. |

The entity question disappeared in this attempt, but seven broader questions
remain unjustified by their supplied diagnoses. A narrower success is not general
reliability improvement. Some fields may require genuine choices on other sources;
do not default every ambiguous finding to supported or invent missing business intent.

### Remaining work and authority boundary

- Extend the retained failure regression to this all-seven, protocol-valid sequence
  through the same review/runner/publication owners. Preserve the distinction between
  structural conformance and semantic necessity; include real missing decisions.
- Review the shared **source-sufficiency assignment** and requirement descriptions
  across all derivable feature fields. The top-level prompt already says to judge
  meaning, not prewritten fields; repeating it or adding a fixture-specific example
  is not an evidenced solution. Assess whether the current per-field tasks faithfully
  express the existing derivation contract before changing prompts or routing.
- If recovery must challenge admitted semantic verdicts, settle D2's focused
  authority decision first. JSON correction and native text/evidence repair cannot
  be repurposed into an unapproved semantic-review loop. A new gate based on words,
  question counts or this source would create brittle duplicate authority.
- Keep Phase 4 live acceptance and Phase 6 closure open. Completion still requires
  full generated AC/FR content, actual principle assessment, persisted validation,
  publication and a recorded rubric grade. Every further live attempt needs new
  explicit approval; this approval is consumed. No second run occurred.
